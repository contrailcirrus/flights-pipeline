name: spire-ingest-resample-worker-deploy-prod

on:
  push:
    branches:
      - main
    paths:
      - spire-ingest-resample-worker/**
      - .github/workflows/spire-ingest-resample-worker-deploy-prod.yaml

# avoid possible race condition on multiple merges
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up GCP auth
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - name: Set up gcloud CLI
        uses: google-github-actions/setup-gcloud@v2

      - name: Set up gcloud plugins
        run: gcloud components install gke-gcloud-auth-plugin

      - name: Authorize kubectl CLI
        run: gcloud container clusters get-credentials contrails-gke-general --region us-east1

      - name: Authorize docker CLI
        run: gcloud auth configure-docker us-east1-docker.pkg.dev

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: spire-ingest-resample-worker/
          push: true
          tags: |
            us-east1-docker.pkg.dev/contrails-301217/flights-pipeline/spire-ingest-resample-worker-prod:${{ github.sha }}
            us-east1-docker.pkg.dev/contrails-301217/flights-pipeline/spire-ingest-resample-worker-prod:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Deploy helm resources
        working-directory: spire-ingest-resample-worker/helm
        run: make deploy
        env:
          NAMESPACE: flights-pipeline-prod
          IMAGE: us-east1-docker.pkg.dev/contrails-301217/flights-pipeline/spire-ingest-resample-worker-prod:${{ github.sha }}
          LOG_LEVEL: WARNING
          SPIRE_INGEST_WAYPOINTS_SUBSCRIPTION_ID: projects/contrails-301217/subscriptions/prod-fp-resample-worker-ingress
          SPIRE_RAW_WAYPOINTS_BIGQUERY_TOPIC_ID: projects/contrails-301217/topics/prod-fp-spire-ingest-raw-bigquery
          SPIRE_WAYPOINTS_BIGQUERY_TOPIC_ID: projects/contrails-301217/topics/prod-fp-spire-ingest-resampled-bigquery
          REDIS_HOST: 10.143.239.108
          REDIS_PORT: 6379
