# image is the docker image uri to be deployed including the image tag
image:

# spireIngestWaypointsSubscriptionId is the fully-qualified uri for the waypoints record pubsub subscription
spireIngestWaypointsSubscriptionId:
# spireWaypointsBigQueryTopicId is the fully-qualifed uri for the pubsub topic that writes to BQ
spireWaypointsBigQueryTopicId:

trajectoryChunkTopicId:

# redisHost is the ipv4 address of the redis instance
redisHost:
# redisPort is the port for the redis instance
redisPort:
