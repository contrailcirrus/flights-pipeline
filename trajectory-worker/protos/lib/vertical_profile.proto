syntax = "proto3";

package lib;

import "lib/segment_reference.proto";

message VerticalProfile {
    uint32 alt_ft = 1;  // encoded as (alt_ft / 16) -- 12bit space
    repeated SegmentReference segment_references = 2;
}
