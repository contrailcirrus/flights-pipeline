# Depends on Google's Custom Metrics Adapter installed by cluster admin.
#
# Docs: https://cloud.google.com/kubernetes-engine/docs/tutorials/autoscaling-metrics#pubsub
#
# Configured in contrails-gke-general, see:
#   https://github.com/contrailcirrus/sre/pull/4

apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: trajectory-worker-gaia-backup-scaler
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: trajectory-worker-gaia-backup
  minReplicas: 1
  maxReplicas: 10
  metrics:
    - type: External
      external:
        metric:
          name: "pubsub.googleapis.com|subscription|num_undelivered_messages"
          selector:
            matchLabels:
              resource.labels.subscription_id: {{ .Values.trajectoryChunkGaiaBackupSubscriptionId | required ".Values.trajectoryChunkGaiaBackupSubscriptionId is required" | splitList "/" | last | quote }}
        target:
          type: Value
          value: 10
